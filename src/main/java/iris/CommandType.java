package iris;

/**
 * Enum representing the types of commands that can be issued by the user.
 */
public enum CommandType {
    ADD_TODO,
    ADD_DEADLINE,
    ADD_EVENT,
    ADD_DO_AFTER,
    FIND,
    DELETE,
    MARK,
    UNMARK,
    LIST,
    BYE,
    INVALID
}
